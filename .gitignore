# OS-specific
.DS_Store
Thumbs.db

# IDEs and editors
.vscode/
.idea/
*.sw?
*.sublime*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables (actual .envs should not be committed)
.env
.env.*

# Python virtualenv (just in case it's created in root)
venv/
__pycache__/
*.py[cod]
*.so

# Node modules at root (if accidentally installed)
node_modules/

# Coverage reports (if running tests from root someday)
coverage/
*.cover
*.py,cover

# Build artifacts (just in case root is used for scripts)
dist/
build/
*.egg-info/

# Mac/Linux system files
ehthumbs.db
Icon?
._*
.Spotlight-V100
.Trashes

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore